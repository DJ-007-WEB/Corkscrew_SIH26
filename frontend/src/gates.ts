import type { GateFamily } from "./types";

export const FAMILY_COLOR: Record<GateFamily, string> = {
  basis: "var(--bp-amber)",
  pauli: "var(--bp-coral)",
  phase: "var(--bp-mint)",
  rotation: "var(--bp-cyan)",
  multi: "var(--bp-violet)",
};
